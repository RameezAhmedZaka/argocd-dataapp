
apiVersion: v1
kind: Service
metadata:
  name: data-app-service
spec:
  selector:
    app: data-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 32000
  type: NodePort
